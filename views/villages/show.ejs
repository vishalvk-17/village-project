<%- layout("/layouts/boilerplate") %>


<%- layout("/layouts/boilerplate") %>

<body class="bg-light">
  <div class="container py-5">
    <h2 class="text-center mb-4">All Villagers</h2>

    <% if (users.length === 0) { %>
      <div class="alert alert-warning text-center">
        No villagers found. Please add a new villager.
      </div>
    <% } else { %>
      <ul class="list-group shadow">
        <% for (let user of users) { %>
          <li class="list-group-item d-flex justify-content-between align-items-center">
            <a href="/<%= user._id %>" class="text-decoration-none fw-bold text-primary">
              <%= user.name %>
            </a>
            <span class="badge bg-secondary rounded-pill">
              Age: <%= user.age %>
            </span>
          </li>
        <% } %>
      </ul>
    <% } %>

    <div class="text-center mt-4">
      <a href="/addUser" class="btn btn-success">
        ➕ Add Villager
      </a>
    </div>
  </div>
</body>




















<!-- <body>
    <h3>All Users</h3>
    <% for(user of users) { %>
        <ul>
            <li><a href="/users/<%= user._id %>"><%= user.name %></a></li>
        </ul>
    <% } %>
    <form method="get" action="/addUser" enctype="multipart/form-data">
        <button>Add user</button>
    </form>
    
</body> -->